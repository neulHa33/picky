import { useState } from 'react';
import type { VoteOption } from '../types/vote';

export function useVoteOptions(initialOptions: VoteOption[]) {
  // TODO: implement add, update, remove logic
  return {};
} 